<Window x:Class="Frame.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="AI Frame"
        Width="1200" Height="800"
        MinWidth="400" MinHeight="300"
        Background="#1e1e1e"
        WindowStyle="None"
        AllowsTransparency="False"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterScreen"
        Loaded="OnLoaded"
        Closing="OnClosing">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome
            CaptionHeight="32"
            GlassFrameThickness="0"
            CornerRadius="0"
            ResizeBorderThickness="6"
            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Tab button -->
        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#888"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Segoe UI Semibold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                BorderBrush="Transparent" BorderThickness="0,0,0,2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#383838"/>
                            </Trigger>
                            <Trigger Property="Tag" Value="active">
                                <Setter Property="Foreground" Value="#e8e8e8"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#0078d4"/>
                                <Setter TargetName="Bd" Property="Background" Value="#2a2a2a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Caption button (minimize / maximize / close) -->
        <Style x:Key="CaptionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#999"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#383838"/>
                                <Setter Property="Foreground" Value="#ddd"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Close button (red on hover) -->
        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource CaptionButtonStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#e81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Title / Tab Bar -->
        <Border Grid.Row="0" Background="#252525">
            <DockPanel>
                <!-- Caption buttons (right side) -->
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <!-- Open folder in Explorer -->
                    <Button Content="📂" Click="OnOpenInExplorerClick"
                            Style="{StaticResource CaptionButtonStyle}" ToolTip="Open folder in Explorer"
                            FontSize="14" FontFamily="Segoe UI Emoji"/>

                    <!-- Open in VS Code Insiders -->
                    <Button Content="&#xE943;" Click="OnOpenInVSCodeInsidersClick"
                            Style="{StaticResource CaptionButtonStyle}" ToolTip="Open in VS Code Insiders"
                            FontSize="14"/>

                    <!-- Separator -->
                    <Border Width="1" Background="#383838" Margin="0,6"/>

                    <Button Content="&#xE921;" Click="OnMinimizeClick"
                            Style="{StaticResource CaptionButtonStyle}" ToolTip="Minimize"/>
                    <Button x:Name="MaximizeButton" Content="&#xE922;" Click="OnMaximizeClick"
                            Style="{StaticResource CaptionButtonStyle}" ToolTip="Maximize"/>
                    <Button Content="&#xE8BB;" Click="OnCloseClick"
                            Style="{StaticResource CloseButtonStyle}" ToolTip="Close"/>
                </StackPanel>

                <!-- Status text (right of center) -->
                <TextBlock x:Name="StatusText" Text="" Foreground="#555" DockPanel.Dock="Right"
                           VerticalAlignment="Center" Margin="0,0,12,0" FontSize="11" FontFamily="Segoe UI"/>

                <!-- Tab buttons (left side) — Copilot first -->
                <StackPanel Orientation="Horizontal" Margin="2,0,0,0" DockPanel.Dock="Left"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button x:Name="CopilotTabButton" Content="★ Copilot" Click="OnCopilotTabClick"
                            Style="{StaticResource TabButtonStyle}" Tag="active"/>
                    <Button x:Name="ConsoleTabButton" Content="⌘ Console" Click="OnConsoleTabClick"
                            Style="{StaticResource TabButtonStyle}" Tag="inactive"/>
                </StackPanel>

                <!-- Folder path (fills remaining center space) -->
                <TextBlock x:Name="FolderPathText" Text="" Foreground="#666"
                           VerticalAlignment="Center" HorizontalAlignment="Center"
                           FontSize="11" FontFamily="Segoe UI"
                           TextTrimming="CharacterEllipsis"/>
            </DockPanel>
        </Border>

        <!-- Terminal Host Panels -->
        <Grid Grid.Row="1" x:Name="TerminalContainer"/>
    </Grid>
</Window>
